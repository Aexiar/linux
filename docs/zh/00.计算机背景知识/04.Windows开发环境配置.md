# 第一章：Windows 常用软件的安装

## 1.1 概述

* 在 `Win10/Win11` 中，可以使用 `winget` 、`scoop` 等软件包管理工具，一键安装常用的软件。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-02/2026-07-02-09-52-01.png)

## 1.2 替换 winget 源和 scoop 源

### 1.2.1 概述

* 默认的 winget 源或 scoop 源很慢，需要替换为国内源。

### 1.2.2 winget 源替换

* 默认的 winget 源很慢，可以替换为国内源：

```powershell
# 先删除较慢的官方 winget 源
winget source remove winget
# 将中科大的镜像源添加进去，并取名为 winget
winget source add winget https://mirrors.ustc.edu.cn/winget-source --trust-level trusted
# 刷新并检查
winget source update
winget source list
```

> [!NOTE]
>
> * ① 修改 WinGet 软件源需要管理员权限，请以管理员身份运行终端。对于 [启用 sudo](https://learn.microsoft.com/windows/advanced-settings/sudo/#how-to-enable-sudo-for-windows) 的 Windows 11 用户，也可以使用 `sudo` 进行提权。
> * ② 重置为官方地址，可以使用如下的命令：
>
> ```powershell
> winget source reset winget
> ```



* 示例：

::: code-group

```powershell
winget source remove winget
winget source add winget https://mirrors.ustc.edu.cn/winget-source --trust-level trusted
winget source update
winget source list
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-02/2026-07-02-19-16-57.gif)
```

:::

### 1.2.3 Scoop 源替换

* 默认的 scoop 源很慢，可以替换为国内源：

```powershell
# 先删除较慢的官方 winget 源
scoop bucket rm main
scoop bucket rm extras
scoop bucket rm versions
scoop bucket rm nerd-fonts
scoop bucket rm nonportable
scoop bucket rm nirsoft
scoop bucket rm java
# 替换为国内镜像源
scoop bucket add main https://mirror.nju.edu.cn/git/scoop-main.git
scoop bucket add extras https://mirror.nju.edu.cn/git/scoop-extras.git
scoop bucket add versions https://mirror.nju.edu.cn/git/scoop-versions.git
scoop bucket add nerd-fonts https://mirror.nju.edu.cn/git/scoop-nerd-fonts.git
scoop bucket add nonportable https://mirror.nju.edu.cn/git/scoop-nonportable.git
scoop bucket add nirsoft https://mirror.nju.edu.cn/git/scoop-nirsoft.git
scoop bucket add java https://mirror.nju.edu.cn/git/scoop-java.git
# 刷新测试
scoop update
```

> [!NOTE]
>
> * ① 可以使用如下的命令，切换 scoop 核心源：
>
> ```powershell
> scoop config scoop_repo https://mirror.nju.edu.cn/git/scoop.git
> ```
>
> * ② 从国内源恢复至官方源：
>
> ```powershell
> scoop bucket rm main
> scoop bucket rm extras
> scoop bucket rm versions
> scoop bucket rm nerd-fonts
> scoop bucket rm nonportable
> scoop bucket rm nirsoft
> scoop bucket rm java
> 
> scoop bucket add main
> scoop bucket add extras
> scoop bucket add versions
> scoop bucket add nerd-fonts
> scoop bucket add nonportable
> scoop bucket add nirsoft
> scoop bucket add java
> ```



* 示例：

::: code-group

```powershell
# 先删除较慢的官方 winget 源
scoop bucket rm main
scoop bucket rm extras
scoop bucket rm versions
scoop bucket rm nerd-fonts
scoop bucket rm nonportable
scoop bucket rm nirsoft
scoop bucket rm java
# 替换为国内镜像源
scoop bucket add main https://mirror.nju.edu.cn/git/scoop-main.git
scoop bucket add extras https://mirror.nju.edu.cn/git/scoop-extras.git
scoop bucket add versions https://mirror.nju.edu.cn/git/scoop-versions.git
scoop bucket add nerd-fonts https://mirror.nju.edu.cn/git/scoop-nerd-fonts.git
scoop bucket add nonportable https://mirror.nju.edu.cn/git/scoop-nonportable.git
scoop bucket add nirsoft https://mirror.nju.edu.cn/git/scoop-nirsoft.git
scoop bucket add java https://mirror.nju.edu.cn/git/scoop-java.git
# 刷新测试
scoop update
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-02/2026-07-02-20-49-59.gif)
```

:::

## 1.3 日常应用安装

### 1.3.1 概述

* 这类软件主要用于日常办公、娱乐、系统维护、硬件管理以及基础的网络和文件处理。

### 1.3.2 浏览器&网络代理

* 格式：

```powershell
# 浏览器
winget install --id Google.Chrome -e
winget install --id Brave.Brave -e
winget install --id Mozilla.Firefox -e
# 网络代理
winget install --id ClashVergeRev.ClashVergeRev -e
winget install --id chen08209.FlClash -e
```



* 示例：安装浏览器

::: code-group

```powershell
winget install --id Google.Chrome -e
winget install --id Brave.Brave -e
winget install --id Mozilla.Firefox -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-02/2026-07-02-18-57-34.gif)
```

:::



* 示例：安装网络代理

::: code-group

```powershell
winget install --id ClashVergeRev.ClashVergeRev -e
winget install --id chen08209.FlClash -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-09-30-30.gif)
```

:::

### 1.3.3 办公&笔记

* 格式：

```powershell
# 办公软件
winget install --id Microsoft.Office --source winget --interactive -e 
winget install --id Kingsoft.WPSOffice.CN --source winget -e

# 笔记软件
winget install --id appmakes.Typora -e
winget install --id Kuingsmile.PicList -e
winget install --id Obsidian.Obsidian -e

# 截图工具
winget install --id Dangbei.XiaowangScreenshot -e 
winget install --id PixPin.PixPin -e 
```



* 示例：安装办公软件

::: code-group

```powershell
winget install --id Kingsoft.WPSOffice.CN --source winget -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-14-54-45.gif)
```

:::



* 示例：安装笔记软件

::: code-group

```powershell
winget install --id appmakes.Typora -e
winget install --id Kuingsmile.PicList -e
winget install --id Obsidian.Obsidian -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-14-56-21.gif)
```

:::



* 示例：安装截图工具

::: code-group

```powershell
winget install --id Dangbei.XiaowangScreenshot -e 
winget install --id PixPin.PixPin -e 
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-14-57-19.gif)
```

:::

### 1.3.4 网盘&下载工具

* 格式：

```powershell
# 网盘
winget install --id Baidu.BaiduNetdisk -e

# 下载工具
winget install --id JavadMotallebi.NeatDownloadManager -e # 需配合对应浏览器插件
```



* 示例：安装网盘

::: code-group

```powershell
winget install --id Baidu.BaiduNetdisk -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-50-09.gif)
```

:::



* 示例：安装下载工具

::: code-group

```powershell
winget install --id JavadMotallebi.NeatDownloadManager -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-51-41.gif)
```

:::

### 1.3.5 远程控制

* 格式：

```powershell
winget install --id NetEase.UURemote -e
```



* 示例：

::: code-group

```powershell
winget install --id NetEase.UURemote -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-52-23.gif)
```

:::

### 1.3.6 视频播放器

* 格式：

```powershell
winget install --id Daum.PotPlayer -e
winget install --id Gyan.FFmpeg -e
winget install --id CreativeTechnology.OpenAL -e
```



* 示例：

::: code-group

```powershell
winget install --id Daum.PotPlayer -e
winget install --id Gyan.FFmpeg -e
winget install --id CreativeTechnology.OpenAL -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-53-12.gif)
```

:::

### 1.3.7 系统工具

* 格式：

```powershell
# 自定义 Windows 的实用工具
winget install --id Microsoft.PowerToys -e
# 免费的 Window 桌面搜索工具
winget install --id voidtools.Everything -e
# 桌面搜索工具
winget install --id ghost-him.ZeroLaunch-rs -e 
# 压缩软件 
winget install --id RARLab.WinRAR -v 7.22.0 --locale zh-sc -e 
winget install --id Giorgiotani.Peazip -e
winget install --id M2Team.NanaZip -e
# Windows 图形化软件包管理器
winget install --id Devolutions.UniGetUI -e
# 输入法
winget install --id Sogou.SogouInput -e
```



* 示例：安装 Windows 实用工具（可选）

::: code-group

```powershell
winget install --id Microsoft.PowerToys -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-55-19.gif)
```

:::



* 示例：安装 Windows 搜索工具（推荐）

::: code-group

```powershell
winget install --id voidtools.Everything -e
winget install --id ghost-him.ZeroLaunch-rs -e 
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-15-33-34.gif)
```

:::



* 示例：安装压缩软件 

::: code-group

```powershell
winget install --id RARLab.WinRAR -v 7.22.0 --locale zh-Hans -e 
winget install --id Giorgiotani.Peazip -e
winget install --id M2Team.NanaZip -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-15-34-08.gif)
```

:::



* 示例：安装 Windows 图形化软件包管理器

::: code-group

```powershell
winget install --id Devolutions.UniGetUI -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-15-34-43.gif)
```

:::



* 示例：安装输入法

::: code-group

```powershell
winget install --id Sogou.SogouInput -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-15-35-13.gif)
```

:::

## 1.4 开发者工具开发

### 1.4.1 概述

* 这类软件主要用于编写代码、管理服务器、数据库操作、环境配置、容器化、CI/CD 以及网络抓包等开发运维场景。

### 1.4.2 Git

* 格式：

```powershell
winget install --id Git.Git --source winget -e 
```



* 示例：

::: code-group

```powershell
winget install --id Git.Git --source winget -e 
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-00-49.gif)
```

:::

### 1.4.3 IDE 或编辑器

* 格式：

```powershell
# 编辑器
winget install --id Notepad++.Notepad++ -e
winget install --id Microsoft.VisualStudioCode -e --scope machine
winget install --id ByteDance.Trae -e
winget install --id SublimeHQ.SublimeText.4 -e
scoop install vim

# IDE
winget install --id Microsoft.VisualStudio.Enterprise -e
winget install --id JetBrains.IntelliJIDEA -e
winget install --id JetBrains.WebStorm -e
winget install --id JetBrains.CLion -e
winget install --id JetBrains.DataGrip -e
winget install --id JetBrains.GoLand -e
winget install --id JetBrains.PyCharm -e
winget install --id JetBrains.RustRover -e
```



* 示例：安装编辑器

::: code-group

```powershell
winget install --id Notepad++.Notepad++ -e
winget install --id Microsoft.VisualStudioCode -e --scope machine
winget install --id ByteDance.Trae -e 
winget install --id SublimeHQ.SublimeText.4 -e
scoop install vim
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-57-33.gif)
```

:::



* 示例：安装 VisualStudio

::: code-group

```powershell
winget install --id Microsoft.VisualStudio.Enterprise -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-15-58-22.gif)
```

:::



* 示例：安装 JetBrains 全家桶

::: code-group

```powershell
winget install --id JetBrains.IntelliJIDEA -e
winget install --id JetBrains.WebStorm -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-00-01.gif)
```

:::

### 1.4.4 虚拟化

* 格式：

```powershell
# 需要开启 CPU 虚拟化
winget install --id Oracle.VirtualBox -e
winget install --id Hashicorp.Vagrant -e
```



* 示例：

::: code-group

```powershell
winget install --id Oracle.VirtualBox -e
winget install --id Hashicorp.Vagrant -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-00-48.gif)
```

:::

### 1.4.5 编程语言环境与工具链

* 格式：

```powershell
# 安装
scoop install mise
```

```powershell
# 激活
# ① 创建目录
# PowerShell 5.1 用户
if (!(Test-Path "$HOME\Documents\WindowsPowerShell")) {
    New-Item -Path "$HOME\Documents\WindowsPowerShell" -ItemType Directory
}
# PowerShell 7+ 用户
if (!(Test-Path "$HOME\Documents\PowerShell")) {
   New-Item -Path "$HOME\Documents\PowerShell" -ItemType Directory
}

# ② 写入 mise 初始化命令
# PowerShell 5.1 用户
"`n`$env:MISE_PWSH_CHPWD_WARNING = 0" | Add-Content $PROFILE
'(&mise activate pwsh) | Out-String | Invoke-Expression' | Add-Content -Path "$HOME\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1"
# PowerShell 7+ 用户
"`n`$env:MISE_PWSH_CHPWD_WARNING = 0" | Add-Content $PROFILE
'(&mise activate pwsh) | Out-String | Invoke-Expression' | Add-Content -Path "$HOME\Documents\PowerShell\Microsoft.PowerShell_profile.ps1"

# ③ 验证配置文件内容
notepad $PROFILE  # (&mise activate pwsh) | Out-String | Invoke-Expression

# ④ 重新加载配置
. $PROFILE
```

> [!NOTE]
>
> 诸如 JDK、Go 、Rust 等开发工具包都可以通过 mise 进行安装！！！



* 示例：安装 mise

::: code-group

```powershell
scoop install mise
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-11-06-23.gif)
```

:::



* 示例：激活 mise

::: code-group

```powershell
# ① 创建目录
# PowerShell 5.1 用户
if (!(Test-Path "$HOME\Documents\WindowsPowerShell")) {
    New-Item -Path "$HOME\Documents\WindowsPowerShell" -ItemType Directory
}
# PowerShell 7+ 用户
if (!(Test-Path "$HOME\Documents\PowerShell")) {
   New-Item -Path "$HOME\Documents\PowerShell" -ItemType Directory
}

# ② 写入 mise 初始化命令
# PowerShell 5.1 用户
"`n`$env:MISE_PWSH_CHPWD_WARNING = 0" | Add-Content $PROFILE
'(&mise activate pwsh) | Out-String | Invoke-Expression' | Add-Content -Path "$HOME\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1"
# PowerShell 7+ 用户
"`n`$env:MISE_PWSH_CHPWD_WARNING = 0" | Add-Content $PROFILE
'(&mise activate pwsh) | Out-String | Invoke-Expression' | Add-Content -Path "$HOME\Documents\PowerShell\Microsoft.PowerShell_profile.ps1"

# ③ 验证配置文件内容
notepad $PROFILE  # (&mise activate pwsh) | Out-String | Invoke-Expression

# ④ 重新加载配置
. $PROFILE
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-11-06-37.gif)
```

:::

### 1.4.6 开发辅助与效率工具

* 格式：

```powershell
# 接口测试工具（UI 界面）
winget install --id Ruihu.Apifox -e
winget install --id Reqable.Reqable -e
winget install --id Apipost.ApiPost -e 

# 抓包工具
winget install --id WiresharkFoundation.Wireshark -e

# Java 分析工具 
winget install --id ej-technologies.JProfiler.15 -e

# 画图工具
winget install --id JGraph.Draw -e

# 接口自动化测试工具
winget install --id Dagger.Cli -e

# 远程连接工具
winget install --id JiLuo.Xterminal -e
scoop install tabby
```



* 示例：安装接口测试工具（UI 界面）

::: code-group

```powershell
winget install --id Ruihu.Apifox -e
winget install --id Reqable.Reqable -e
winget install --id Apipost.ApiPost -e 
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-03-07.gif)
```

:::



* 示例：安装抓包工具

::: code-group

```powershell
winget install --id WiresharkFoundation.Wireshark -e 
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-03-56.gif)
```

:::



* 示例：安装 Java 分析工具 

::: code-group

```powershell
winget install --id ej-technologies.JProfiler.15 -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-04-31.gif)
```

:::



* 示例：安装画图工具

::: code-group

```powershell
winget install --id JGraph.Draw -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-05-07.gif)
```

:::



* 示例：安装接口自动化测试工具

::: code-group

```powershell
winget install --id Dagger.Cli -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-05-43.gif)
```

:::



* 示例：安装远程连接工具

::: code-group

```powershell
winget install --id JiLuo.Xterminal -e
scoop install tabby
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-08-08.gif)
```

:::

### 1.4.7 命令行与终端增强

* 格式：

```powershell
# 命令行增强工具
winget install JanDeDobbeleer.OhMyPosh --source winget -e
winget install --id chrisant996.Clink -e
winget install --id Fastfetch-cli.Fastfetch -e
winget install --id lsd-rs.lsd -e
# 字体
scoop install Meslo-NF 
scoop install Meslo-NF-Propo
scoop install Meslo-NF-Mono
scoop install Cascadia-Code
scoop install firacode
```



* 示例：安装字体

::: code-group

```powershell
scoop install Meslo-NF 
scoop install Meslo-NF-Propo
scoop install Meslo-NF-Mono
scoop install Cascadia-Code
scoop install firacode
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-10-17.gif)
```

:::



* 示例：安装命令行增强工具

::: code-group

```powershell
winget install JanDeDobbeleer.OhMyPosh --source winget -e
winget install --id chrisant996.Clink -e
winget install --id Fastfetch-cli.Fastfetch -e
winget install --id lsd-rs.lsd -e
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-03/2026-07-03-16-11-44.gif)
```

:::



# 第二章：Tabby

## 2.1 概述

* [Tabby](https://github.com/eugeny/tabby) 是一款现代化、高度可配置的 `跨平台终端工具`，其前身叫做 Terminus 。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-04-55.png)

* 其主要作用，如下所示：

| 主要作用            | 描述                                                         |
| ------------------- | ------------------------------------------------------------ |
| `全能的连接管理`    | 它不仅仅是一个本地命令行终端，还内置了强大的 SSH 客户端、串口（Serial）连接和 Telnet 支持。如果需要远程连接服务器或管理网络设备，它是一个非常方便的集成工具。 |
| `高度可定制化`      | Tabby 提供了极其丰富的主题、配色方案和快捷键自定义功能，您可以根据自己的喜好打造个性化的终端界面 。 |
| `跨平台与现代化 UI` | 完美支持 Windows 11、macOS 和 Linux，拥有现代化的 UI 设计，支持分屏（Split Panes）、标签页管理以及插件扩展系统。 |
| `便捷的功能`        | 内置了 SFTP 文件传输、ZMODEM 支持、以及保存 SSH 密码/密钥的连接管理器，极大提升了运维和开发效率。 |

## 2.2 安装和配置

### 2.2.1 安装

* 格式：

```powershell
scoop install tabby
```



* 示例：

::: code-group

```powershell
scoop install tabby
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-12-05.gif)
```

:::

### 2.2.2 配置

* ① 在 `设置` --> `应用` 中，打开 `Shell 集成` 。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-19-45.gif)

* ② 在 `设置` --> `外观` --> `自定义 CSS` 中，设置如下的代码，以便隐藏滚动条：

::: code-group

```css
/* 全局隐藏 Tabby 的所有滚动条 */
::-webkit-scrollbar, 
::-webkit-scrollbar-button, 
::-webkit-scrollbar-thumb, 
::-webkit-scrollbar-track, 
::-webkit-scrollbar-track-piece {
    width: 0 !important;
    height: 0 !important;
    background: transparent !important;
    display: none !important;
}
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-27-37.gif)
```

:::

* ③ `设置` --> `外观` 中，设置 `FiraCode Nerd Font` ，前提是安装 `FiraCode`  字体：

::: code-group

```powershell
scoop install firacode
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-30-14.gif)
```

:::

* ④ 在 `设置` --> `终端` 中，关闭 `启动` 中的设置。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-42-48.gif)

* ⑤ 在 `设置` --> `插件` 中，安装 `background`、`config-roam`、`connection-homepage` 等插件。

> [!NOTE]
>
> * ① `background` 插件，是用来设置背景图片的，所选择的图片最好是 png 格式的透明图片。
> * ② `config-roam` 插件，是用来方便迁移 Tabby 配置的，是通过 s3 协议的。
> * ③ `connection-homepage` 插件，是用来形成 `连接的首页`，并简化 `连接`（远程） 操作。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-54-31.gif)

* ⑥ 配置 `background` 插件，如下所示

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-09-07-26.gif)

* ⑦ 配置`config-roam` 插件，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-09-08-52.gif)
