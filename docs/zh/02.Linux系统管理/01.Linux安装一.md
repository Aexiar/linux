# 第一章：环境准备

## 1.1 概述

* `VMware Workstation` 是由全球知名虚拟化软件公司 VMware 开发的一款 `桌面端虚拟机软件`。

> [!NOTE]
>
> * ① 简单来说，它就像是一个 “系统多开器”，允许在现有的操作系统（Windows 或 Linux）中，像运行普通软件一样，同时安装和运行多个独立的其他操作系统（如多个版本的 Windows、Linux、MacOS 等）。
> * ② 这些在软件内运行的系统被称为 “虚拟机（Virtual Machine, VM）”，而原本的电脑系统被称为 “宿主机（Host）”。

* VMware Workstation 在软件开发、系统测试、网络工程等领域被广泛使用，如下所示：

| 作用                     | 描述                                                         |
| ------------------------ | ------------------------------------------------------------ |
| ① 多系统并行运行         | 无需重新启动电脑，就可以在 Windows 10/11 上同时运行 Ubuntu、CentOS、Windows 7 等多个系统，方便进行跨平台操作。 |
| ② 安全隔离与软件测试     | 虚拟机与宿主机之间是隔离的。可以在虚拟机里测试未知软件、打开可疑链接或运行病毒样本，即使虚拟机彻底崩溃或中毒，也不会对你的真实电脑造成任何影响。 |
| ③ 快照（Snapshot）与克隆 | 这是最强大的功能之一。可以为虚拟机的某个状态创建“快照”（类似游戏存档）。如果后续操作失误导致系统搞砸了，只需一键就能“读档”恢复到快照时的完好状态。 |
| ④ 模拟复杂的网络环境     | 它可以虚拟出多块网卡、交换机、路由器，让多台虚拟机组成一个复杂的局域网，非常适合网络工程师用来测试网络拓扑、服务器集群或进行渗透测试。 |
| ⑤ 开发与搭建开发环境     | 开发者可以在虚拟机中配置干净的开发环境（Docker、Kubernetes、各种数据库），避免污染宿主机，且方便打包分发给团队其他成员。 |

* VMware Workstation 的核心技术是虚拟化，其是基于 Type-2 型 Hypervisor ，如下所示：

> [!NOTE]
>
> ::: details 点我查看 具体底层工作原理
>
> * ① **硬件资源层（Hardware）：** 真实电脑硬件（CPU、内存、硬盘、显卡）。
> * ② **宿主操作系统层（Host OS）：** 平时用的 Windows 或 Linux 系统。
> * ③ **VMware 软件层（Hypervisor）：** 运行在宿主系统之上的软件。它扮演着 `“中间协调官”` 和  `“魔术师”` 的角色：
>   * **硬件抽象与拦截：** 它把宿主机的物理硬件资源（16核 CPU、32G 内存）切分成很多虚拟的碎片。当虚拟机里的系统（Guest OS）发出读取硬盘或计算指令时，VMware 会拦截这些指令，并将其翻译成宿主机能理解的普通软件请求，再提交给宿主机处理。
>   * **VMM（虚拟机监视器）：** 为每个虚拟机创造一个全虚拟的硬件环境（虚拟 CPU、虚拟内存、虚拟网卡等）。虚拟机里的系统根本不知道自己运行在软件里，它以为自己独占了一台真实的电脑。
> * ④ **客户操作系统层（Guest OS）：** 运行在虚拟机内部的系统。它们相互隔离，互不干扰，通过 VMware 软件安全地共享宿主机的物理资源。
>
> :::

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-08-47-38.svg)

## 1.2 安装 VMware Workstation 

* ① 双击打开 [VMware Workstation](https://drive.google.com/file/d/1itx6d7E52lVDWXNUtq5Y9bOvIXV-WaTN/view?usp=sharing) 安装程序，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-08-54-00.gif)

* ② 点击 `Next` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-08-55-06.gif)

* ③ 勾选 `我接受许可协议中的条款` ，并点击 `Next` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-01-12.gif)

* ④ 点击 `Next` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-02-18.gif)

* ⑤ 根据需要更改软件的安装位置，点击 `Next` ，如下所示：

> [!NOTE]
>
> 默认即可，软件本身并不大！！！

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-04-30.gif)

* ⑥ 将 `每日更新` 等关闭，点击 `Next` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-05-58.gif)

* ⑦ 点击 `Next` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-06-55.gif)

* ⑧ 点击 `Install` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-10-17.gif)

* ⑨ 点击 `Finish` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-09-10-11.gif)

* ⑩ 默认显示界面是 `英文`，可以配置为 `中文` ，如下所示：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-08-47.gif)

## 1.3 虚拟机网络环境

### 1.3.1 虚拟机网络服务

* 安装完 VMware Workstation 之后，在 Windows 的服务管理中，会存在一些相关的服务：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-14-20.gif)

* VMware Workstation 服务启动之后，会开启两个虚拟网卡：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-18-08.gif)

### 1.3.2 虚拟机网络模式







### 1.3.3 虚拟机快照





### 1.3.4 虚拟机克隆





## 1.4 终端工具（Tabby）

### 1.4.1 概述

* [Tabby](https://github.com/eugeny/tabby) 是一款现代化、高度可配置的 `跨平台终端工具`，其前身叫做 Terminus 。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-31.png)

* 其主要作用，如下所示：

| 主要作用            | 描述                                                         |
| ------------------- | ------------------------------------------------------------ |
| `全能的连接管理`    | 它不仅仅是一个本地命令行终端，还内置了强大的 SSH 客户端、串口（Serial）连接和 Telnet 支持。如果需要远程连接服务器或管理网络设备，它是一个非常方便的集成工具。 |
| `高度可定制化`      | Tabby 提供了极其丰富的主题、配色方案和快捷键自定义功能，您可以根据自己的喜好打造个性化的终端界面 。 |
| `跨平台与现代化 UI` | 完美支持 Windows 11、macOS 和 Linux，拥有现代化的 UI 设计，支持分屏（Split Panes）、标签页管理以及插件扩展系统。 |
| `便捷的功能`        | 内置了 SFTP 文件传输、ZMODEM 支持、以及保存 SSH 密码/密钥的连接管理器，极大提升了运维和开发效率。 |

### 1.4.2 安装

* 格式：

```powershell
scoop install tabby
```



* 示例：

::: code-group

```powershell
scoop install tabby
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-12-05.gif)
```

:::

### 1.4.3 修改默认配置

* ① 在 `设置` --> `应用` 中，打开 `Shell 集成` 。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-35.gif)

* ② 在 `设置` --> `外观` --> `自定义 CSS` 中，设置如下的代码，以便隐藏滚动条：

::: code-group

```css
/* 全局隐藏 Tabby 的所有滚动条 */
::-webkit-scrollbar, 
::-webkit-scrollbar-button, 
::-webkit-scrollbar-thumb, 
::-webkit-scrollbar-track, 
::-webkit-scrollbar-track-piece {
    width: 0 !important;
    height: 0 !important;
    background: transparent !important;
    display: none !important;
}
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-27-37.gif)
```

:::

* ③ `设置` --> `外观` 中，设置 `FiraCode Nerd Font` ，前提是安装 `FiraCode`  字体：

::: code-group

```powershell
scoop install firacode
```

```md:img [cmd 控制台]
![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-04/2026-07-04-08-30-14.gif)
```

:::

* ④ 在 `设置` --> `终端` 中，关闭 `启动` 中的设置。

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-33.gif)

### 1.4.4 安装插件

#### 1.4.4.1 background

* 在 `设置` --> `插件` 中，安装 `background` 插件，其是用来设置背景图片。

> [!NOTE]
>
> 选择的图片应该是背景透明的 png 格式的图片！！！



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-42.gif)

#### 1.4.4.2 config-roam 

* 在 `设置` --> `插件` 中，安装 `config-roam` 插件，以便迁移 Tabby 配置。

> [!NOTE]
>
> 此插件支持 s3 协议，所以大部分公有云的 oss 都支持，自建兼容 s3 协议的 oss 也可以！！！



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-31.gif)

#### 1.4.4.3 connection-homepage

* 在 `设置` --> `插件` 中，安装 `connection-homepage` 插件，是用来形成 `连接主页` 界面。

> [!NOTE]
>
> 此插件可以简化 `连接` 操作！！！



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-39.gif)

#### 1.4.4.4 highlight-pane

* 在 `设置` --> `插件` 中，安装 `highlight-pane` 插件，可以对激活的窗口进行 `左`、`右`、`上`、`下` 切分。

> [!NOTE]
>
> 此插件目前只支持 `SSH` 窗口！！！



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-47.gif)

#### 1.4.4.5 sftp-plus

* 在 `设置` --> `插件` 中，安装 `sftp-plus` 插件，其对 Tabby 默认的 `sftp` 功能进行增强。

> [!NOTE]
>
> 此插件在 `上传` 或 `下载` 的时候，支持拖拽！！！



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-52.gif)

#### 1.4.4.6 quick-command-plus

* 在 `设置` --> `插件` 中，安装 `quick-command-plus` 插件，对 `命令` 进行保存，以便下次使用。



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-39-02.gif)

#### 1.4.4.7 search-in-browser

* 在 `设置` --> `插件` 中，安装 `search-in-browser` 插件，以便对 `命令` 等进行浏览器搜索。



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-56.gif)

#### 1.4.4.8 server-stats

* 在 `设置` --> `插件` 中，安装 `server-stats` 插件，以便展示服务器状态，如：CPU 等。



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-38-57.gif)

#### 1.4.4.9 trzsz

* 在 `设置` --> `插件` 中，安装 `trzsz` 插件，以便支持 `rz` 或 `sz` 等命令。



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-39-00.gif)

#### 1.4.4.10 docker-console

* 在 `设置` --> `插件` 中，安装 `docker-console` 插件，以便对 `容器`、`镜像` 等进行快速操作。



* 示例：

![](https://github-pic.oss-cn-hangzhou.aliyuncs.com/linux/2026-07-14/2026-07-14-10-39-06.gif)



# 第二章：安装 RockyLinux

 







